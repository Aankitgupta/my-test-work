# White Board dashboard redesign

This repository contains a standalone static prototype that recreates the provided dashboard in a cleaner, more insight-focused layout.

## Preview locally

Open `index.html` in a browser, or serve the directory:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
