# Sales Dashboard

A responsive, static sales dashboard that highlights revenue performance, customer growth, win rate, channel mix, regional attainment, pipeline opportunities, and top-selling products.

## Run locally

Open `index.html` in a browser, or serve the directory with any static file server:

```bash
python3 -m http.server 8080
```

Then visit `http://localhost:8080`.
